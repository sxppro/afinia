{
  "name": "afinia-ingest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:seed": "sst shell --stage dev tsx ./src/provider/up/modules/loadData.ts",
    "db:generate": "sst shell --stage dev drizzle-kit generate",
    "db:migrate": "sst shell --stage dev drizzle-kit migrate",
    "dev": "sst secret load ./.env --stage dev && sst dev --stage dev",
    "load-env": "sst secret load ./.env --stage dev",
    "sso": "aws sso login --sso-session=soppro"
  },
  "dependencies": {
    "afinia-common": "workspace:*",
    "drizzle-orm": "^0.44.6",
    "openapi-fetch": "^0.15.0",
    "postgres": "^3.4.7",
    "sst": "3.17.19"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.155",
    "@types/node": "^20",
    "drizzle-kit": "^0.31.5",
    "tsx": "^4.20.6"
  }
}